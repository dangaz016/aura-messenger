# Backend API URL — for production deployment.
# Leave empty for local dev (Vite proxies /api -> localhost:3001).
# In production (Netlify), set this to your backend URL, e.g.:
# VITE_API_URL=https://aura-backend.onrender.com

VITE_API_URL=
