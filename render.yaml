# Render.com deployment config — repo root
services:
  - type: web
    name: aura-server
    runtime: node
    rootDir: server
    buildCommand: npm --prefix ../client install && npm --prefix ../client run build && npm install && npm run build
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_VERSION
        value: "22.11.0"
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGINS
        value: "*"
      - key: PORT
        value: "10000"
      - key: GROQ_API_KEY
        sync: false
